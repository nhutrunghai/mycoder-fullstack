apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-home-local-path-pvc
  namespace: jenkins
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: jenkins-local-path-retain
  resources:
    requests:
      storage: 10Gi
