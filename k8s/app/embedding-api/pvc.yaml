apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: embedding-model-data
  namespace: mycoder-dev
  labels:
    app: mycoder-embedding-api
    tier: ai
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
